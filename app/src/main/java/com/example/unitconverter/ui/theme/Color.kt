package com.example.unitconverter.ui.theme

import androidx.compose.ui.graphics.Color

// --- New Cyan Palette ---
val CyanPrimary = Color(0xFF00FFFF)   // Bright Cyan (for primary active elements)
val CyanDark = Color(0xFF00CCCC)     // Darker Cyan (for secondary elements)

// --- New Black Palette (for Dark Theme) ---
val BlackBackground = Color(0xFF000000) // Pure Black background
val DarkSurface = Color(0xFF121212)     // Near-black surface for elevated cards
val OnDark = Color(0xFFFFFFFF)          // White text/icons on dark surfaces